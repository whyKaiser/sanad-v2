# تحقق إصدار سَنَد ٢

التاريخ: 14 سبتمبر 2026. الاختبارات لا تعني ضمان خلو البرنامج من كل عيب، ولا اعتمادًا أمنيًا حكوميًا.

| الفحص | النتيجة |
|---|---|
| TypeScript | نجاح، بلا أخطاء أنواع |
| اختبارات الوحدة والمنطق وOCR وقاعدة البيانات | 55 نجحت، صفر فشل؛ تشمل اختبارات واجهة الصلاحيات |
| اختبارات التكامل على البناء وقاعدة معزولة | 20 نجحت، صفر فشل |
| بناء الإنتاج وفحص أسراره | نجاح |
| ESLint | صفر أخطاء؛ 38 تنبيهًا غير مانع عن imports غير مستخدمة، استبعاد حقول عند التفكيك، وعرض الصور الأصلية |
| قاعدة D1 المستقلة | جميع migrations مطبقة، بما فيها triggers يقين |
| الدخول على الرابط المنشور | الأدوار الأربعة تعمل، ومنع الوصول غير المخوّل متحقق |
| البيانات المنشورة | ست حالات اصطناعية؛ لا نسخ من بيانات المراجع الحكومية |
| يقين المنشور | 30 حدثًا موقعًا؛ لا توقيعات معلقة. فحص السلسلة ومطابقة البيانات نجح |
| Groq المنشور | مفعّل بطلب صاحب المشروع؛ أعاد إجابة generative مع خمسة مراجع بعد نشر التحديث. المفتاح محفوظ على الخادم |
| الواجهة | فحص المسؤول وحساب العرض على سطح المكتب و390×844؛ قراءة خمسة حقول داخل المتصفح، وإخفاء التعديل حسب الدور، وفتح تفاصيل الحركة |
| المصدر | فحص عدم تضمين قيم الأسرار وكلمات مرور حسابات العرض |

أهم سيناريوهات التكامل: حفظ وثيقة واسترجاعها، ملف 5MB، منع التكرار، رفض رقم مراجعة قديم، لقطة حزمة ثابتة، تواريخ الدخول والخروج، ما قبل الوصول، الصلاحيات، إبطال جاهزية تغيّر ملفها، حفظ افتراضات القرار، وإلغاء الجلسة فورًا. اختبارات SQLite تتحقق من حفظ قبل/بعد مع العملية ومنع الكتابة بلا سياق ومنع تعديل سجل يقين أو حذفه.

معرف نشر Cloudflare: `84b2e759-3c3e-41de-b3f2-8005e1467bb6`.

الأصل محفوظ في مجلده وWorker وقاعدته. مرجعه أثناء التحقق: `934aa93fbb2032875ee68b3fd6c08bc19eba6344`، وشجرة عمله نظيفة.

الملفات داخل `work` خاصة ولا تدخل المستودع أو ZIP. يتضمن `docs/verify-workflow.example.yml` قالبًا اختياريًا لتشغيل الاختبارات في GitHub Actions. لم يُفعّل تلقائيًا لأن اعتماد GitHub الحالي لا يملك صلاحية `workflow`. الاختبارات الموثقة أعلاه محلية وعلى الموقع المنشور وليست نتيجة CI.

اختُبر كذلك تحميل النموذج الدلالي المحلي من الواجهة وظهر بحالة مفعّل دون مفتاح API. أُصلح فهم المقارنة العامية «وين يختلف المكتوب في نسخة المستند عن معلومات الوصول»، وأضيف اختبار يمنع تفسيرها كطلب عرض جواز فقط؛ تحققت الإجابة الصحيحة في الواجهة وعلى الموقع المنشور.


## نتائج تحديث القراءة

75 اختبارًا ناجحًا بالمجموع: 55 وحدة/منطق/OCR/واجهة/SQLite، و20 تكاملًا على البناء النهائي وقاعدة معزولة. OCR الفعلي نجح في الحقول الخمسة على أربع صور اصطناعية: عمودان، سطور بعناوين بديلة، JPEG بجودة 62، وPNG مصغّر. اختبارات إضافية ترفض اليوم غير الصحيح والتاريخ الملتبس وتسرب عنوان الحقل التالي إلى القيمة. لا يدل ذلك على دعم كل الجوازات أو اللغات.

اختبارات الواجهة تتحقق من غياب أدوات التعديل لدى العرض، وبقاء الوصول للمستند، والفصل بين مسودة موظف المعالجة واعتماد المشرف. التكامل يختبر رفض طلبات كتابة العرض واعتماد موظف غير مخوّل، وتشخيص حالة إعداد AI دون إظهار المفتاح.

كشف فحص حي للمساعد صياغة توحي بإصدار وثيقة بعد اعتماد المراجعة، وتسمية غير صحيحة للجواز. شُددت تعليمات الصياغة، وأضيف فحص خادمي يرفض أنماط الإصدار والإبعاد وتلك التسمية ويعيد إجابة السجل. اختبار انحدار يغطي أربع صيغ، منها التشكيل والعربية والإنجليزية. الفحص يعالج أنماطًا محددة ولا يضمن كشف كل خطأ دلالي؛ المراجع والإجابة الأصلية ومراجعة الموظف تظل لازمة. أعاد Groq وضع `generative` مجددًا بعد النشر النهائي بخمسة مراجع.

## قياس تقني على الرابط المنشور

وقت القياس: 2026-09-14T11:15:10.043Z. ست حالات اصطناعية في المساحة؛ خمس طلبات متتابعة لكل عملية على ملف واحد.

| العملية | الوسيط بالمللي ثانية | الأقل–الأعلى |
|---|---|---|
| قراءة بيانات الملف | 315 | 300–332 |
| استرجاع نسخة الوثيقة | 328 | 290–358 |
| تجهيز وتنزيل HTML الحزمة | 322 | 296–517 |

تشمل الأوقات الشبكة وتنزيل الرد، ولا تشمل رسم الواجهة أو المراجعة البشرية. عينة واحدة صغيرة؛ ليست اختبار حمل أو دليلًا على تقليل مدة التوقيف، ولا يوجد خط أساس ميداني للمقارنة. لإعادة القياس: `npm run benchmark` كما هو موضح في [شرح التحديث](V2-IMPROVEMENTS.md).
