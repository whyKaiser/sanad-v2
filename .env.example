# Optional. Keep the real key in an ignored .env locally, or a Cloudflare runtime secret.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b

# Created with npm run setup:login; the generated password is saved only under work/.
SANAD_USERNAME=admin
SANAD_PASSWORD_HASH=
SANAD_DISPLAY_NAME=مراجع سَنَد
